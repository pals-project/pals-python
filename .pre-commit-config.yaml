repos:
- repo: https://github.com/astral-sh/ruff-pre-commit
  # Ruff version
  rev: v0.15.7
  hooks:
    # Run the linter
    - id: ruff-check
      args: [--fix, --exit-non-zero-on-fix]
    # Run the formatter
    - id: ruff-format
